<table style="width: 100%; font-family: finder">
    <tr>
        <td style="width: 66%">
            <table style="width: 100%; text-align: right">
                <tr>
                    <td style="font-size: 30pt">{{ title }}</td>
                </tr>

                <tr>
                    <td style="font-size: 20pt">{{ subtitle }}</td>
                </tr>

                <tr>
                    <td style="font-family: figtree">{{ fromDate }} et {{ toDate }}</td>
                </tr>
            </table>

            <table>
                <tr>
                    <td style="font-size: 20pt">PASS MEDIA #{{ media.id }}</td>
                </tr>

                <tr>
                    <td style="font-size: 30pt">{{ media.person.firstName }}</td>
                </tr>

                <tr>
                    <td style="font-size: 30pt">{{ media.person.lastName }}</td>
                </tr>
            </table>
        </td>


        <td style="width: 34%">
            <img src="{{ logoPath }}" height="250"/>
        </td>
    </tr>
</table>

